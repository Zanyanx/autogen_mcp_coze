services:
  - type: web
    name: autogen-agent-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn autogen_async_api:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
    plan: free
